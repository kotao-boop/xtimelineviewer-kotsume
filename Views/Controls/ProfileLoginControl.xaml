<UserControl
    x:Class="XTimelineViewer.Views.Controls.ProfileLoginControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <!-- Phase 1: Login -->
        <Grid x:Name="LoginPhase">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Padding="16,8" ColumnSpacing="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="LoginHintText"
                           VerticalAlignment="Center"
                           FontSize="14"
                           Opacity="0.7"/>
                <Button x:Name="ManualCheckBtn" Grid.Column="1" Click="ManualCheckBtn_Click"/>
            </Grid>
            <InfoBar x:Name="SocialSignInNotice"
                     Grid.Row="1"
                     IsOpen="True"
                     IsClosable="False"
                     Severity="Informational"
                     Margin="12,0,12,8"/>
            <WebView2 x:Name="LoginWebView"
                      Grid.Row="2"
                      xmlns="using:Microsoft.UI.Xaml.Controls"/>
        </Grid>

        <!-- Phase 2: Confirm -->
        <StackPanel x:Name="ConfirmPhase"
                    Visibility="Collapsed"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Spacing="16"
                    Padding="32">
            <TextBlock x:Name="DetectedText"
                       FontSize="16"
                       FontWeight="SemiBold"
                       HorizontalAlignment="Center"/>
            <TextBox x:Name="ProfileNameBox"
                     Width="280"
                     HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
