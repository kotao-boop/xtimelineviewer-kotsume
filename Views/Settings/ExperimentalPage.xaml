<Page
    x:Class="XTimelineViewer.Views.Settings.ExperimentalPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls">

    <ScrollViewer Padding="24,16">
        <StackPanel Spacing="4">
            <TextBlock x:Name="PageTitle"
                       Style="{StaticResource TitleTextBlockStyle}"
                       Margin="0,0,0,16"/>

            <!-- 注意喚起（#242）：ここの機能は予告なく変更・非推奨・削除されうる -->
            <InfoBar x:Name="CautionBar"
                     Severity="Warning"
                     IsOpen="True"
                     IsClosable="False"
                     Margin="0,0,0,8"/>

            <!-- Compose Window Preload（#244 案B）-->
            <controls:SettingsCard x:Name="ComposePreloadCard">
                <controls:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xE70F;" FontFamily="Segoe Fluent Icons"/>
                </controls:SettingsCard.HeaderIcon>
                <ToggleSwitch x:Name="ComposePreloadToggle"
                              IsOn="{x:Bind VM.ComposePreloadEnabled, Mode=TwoWay}"/>
            </controls:SettingsCard>

            <!-- 投稿後にプライマリプロフィールへ戻す（#285）-->
            <controls:SettingsCard x:Name="ComposeResetToPrimaryCard">
                <controls:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xE77B;" FontFamily="Segoe Fluent Icons"/>
                </controls:SettingsCard.HeaderIcon>
                <ToggleSwitch x:Name="ComposeResetToPrimaryToggle"
                              IsOn="{x:Bind VM.ComposeResetToPrimaryEnabled, Mode=TwoWay}"/>
            </controls:SettingsCard>

            <!-- 画像表示中のペインを一時拡大（#287）-->
            <controls:SettingsCard x:Name="MediaEnlargeCard">
                <controls:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xE740;" FontFamily="Segoe Fluent Icons"/>
                </controls:SettingsCard.HeaderIcon>
                <ToggleSwitch x:Name="MediaEnlargeToggle"
                              IsOn="{x:Bind VM.MediaEnlargeEnabled, Mode=TwoWay}"/>
            </controls:SettingsCard>

            <!-- 動画の全画面ボタンでペインを一時拡大（#289）-->
            <controls:SettingsCard x:Name="VideoEnlargeCard">
                <controls:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xE740;" FontFamily="Segoe Fluent Icons"/>
                </controls:SettingsCard.HeaderIcon>
                <ToggleSwitch x:Name="VideoEnlargeToggle"
                              IsOn="{x:Bind VM.VideoEnlargeEnabled, Mode=TwoWay}"/>
            </controls:SettingsCard>

            <!-- メディアに自前の拡大ボタンを重ねる（#293）-->
            <controls:SettingsCard x:Name="MediaOverlayButtonCard">
                <controls:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xE740;" FontFamily="Segoe Fluent Icons"/>
                </controls:SettingsCard.HeaderIcon>
                <ToggleSwitch x:Name="MediaOverlayButtonToggle"
                              IsOn="{x:Bind VM.MediaOverlayButtonEnabled, Mode=TwoWay}"/>
            </controls:SettingsCard>

            <!-- 画像・GIF・動画をファイル保存（#299/#304/#308）。説明文に保存先フォルダーの inline リンク（#312）-->
            <controls:SettingsCard x:Name="VideoFrameSaveCard">
                <controls:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xE722;" FontFamily="Segoe Fluent Icons"/>
                </controls:SettingsCard.HeaderIcon>
                <ToggleSwitch x:Name="VideoFrameSaveToggle"
                              IsOn="{x:Bind VM.VideoFrameSaveEnabled, Mode=TwoWay}"/>
            </controls:SettingsCard>

            <controls:SettingsCard x:Name="BossModeCard">
                <controls:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xE7B7;" FontFamily="Segoe Fluent Icons"/>
                </controls:SettingsCard.HeaderIcon>
                <StackPanel Spacing="6">
                    <ToggleSwitch x:Name="BossModeToggle"
                                  IsOn="{x:Bind VM.BossModeButtonVisible, Mode=TwoWay}"/>
                    <Button x:Name="BossModeImageButton" Click="BossModeImageButton_Click"/>
                    <TextBlock x:Name="BossModeImageLabel" FontSize="12" Opacity="0.7" TextTrimming="CharacterEllipsis"/>
                </StackPanel>
            </controls:SettingsCard>
            <!-- ［…］メニューに「直前のリポストを検索」を追加（#315）-->
            <controls:SettingsCard x:Name="PriorRepostSearchCard">
                <controls:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xE721;" FontFamily="Segoe Fluent Icons"/>
                </controls:SettingsCard.HeaderIcon>
                <ToggleSwitch x:Name="PriorRepostSearchToggle"
                              IsOn="{x:Bind VM.PriorRepostSearchEnabled, Mode=TwoWay}"/>
            </controls:SettingsCard>
        </StackPanel>
    </ScrollViewer>
</Page>
